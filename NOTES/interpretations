Yes, and that expectation is reasonable.

What your current analyses suggest is:

in weight space, News looks broader and less similar to public
in eval, News seems to need a lower rank than Math and Academic
Yes, there are some common patterns, just not a single clean one that immediately gives the “right” rank by itself.

What seems fairly consistent in your data:

News is farther from public than Math and Academic

lower public_expert_cosine
higher relative_delta_norm
higher delta_norm_fro
News also looks less compressible spectrally

lower top50_energy_share
higher energy_rank
broader spread across directions
Math and Academic look more similar to each other

both in layerwise weight-similarity curves
and in the SVD-side summaries
projection matters

gate_proj often seems more behaviorally distinctive
down_proj and up_proj often dominate mean behavior late
the projections should not be collapsed too early
layer position matters

late-layer behavior is not the same across experts
News often separates more clearly there
So there is a pattern:

bigger departure from public tends to line up with broader spectrum and higher spectral rank
That is useful for post hoc rank selection.

What it suggests for improving effective-rank selection post hoc:

Stop using one global expert-wide rank
use per-matrix or at least per-projection/per-layer rank signals
Use a hybrid score, not pure entropy/SVD alone
combine:
relative_delta_norm
public_expert_cosine
an SVD metric like energy_rank_095
intuition:
far from public + broad spectrum => higher rank
close to public + concentrated spectrum => lower rank
Treat projections differently
especially allow gate_proj to behave differently from down/up
projection-specific rank buckets may work better than one shared rank
Treat late layers differently
if the layerwise curves systematically diverge near the end, rank allocation should reflect that
A simple post hoc heuristic to try would be:

compute per matrix:
score = a * normalized(relative_delta_norm) + b * normalized(1 - public_expert_cosine) + c * normalized(energy_rank_095)
then map score into rank buckets:
low score -> low rank
medium score -> medium rank
high score -> high rank
That would use the common patterns you found instead of relying on one unstable threshold.

So the answer is:

yes, there are common patterns
they point toward hybrid, matrix-aware rank allocation
especially projection-aware and layer-aware
not one pure SVD rule and not one expert-wide rank
If you want, I can make a first concrete hybrid rank-allocation table next.


So the tension is real:

structural rank signals point one way
functional/eval rank signals point another way
I think the most plausible interpretation is:

Math and Academic may need more rank because their useful adaptation is spread across more directions that the benchmark actually uses
News may have a broad update in weight space, but only a smaller subset of that update is actually useful for the eval tasks
so News can look spectrally broad while still performing best at low rank
In other words:

weight-space breadth != task-relevant breadth
That would explain why you’d still expect:

higher rank for Math
higher rank for Academic
lower rank for News
even though News does not look low-rank under the SVD-only analyses.

So the current lesson may be:

SVD-based methods are capturing structural complexity
eval is capturing useful complexity
for News, those are not the same thing
That means if your true target is post hoc rank allocation that matches benchmark behavior, then a better method may need to incorporate:

domain/task identity
activations
or some empirical calibration against eval
rather than relying on singular values alone.

A strong way to phrase the current finding would be:

Although the News expert appears less similar to the public model and more spectrally diffuse than Math and Academic, downstream evaluation suggests that its task-useful adaptation is still captured well at much lower rank. This indicates that structural breadth in weight space does not necessarily translate into a need for higher effective adaptation rank.

So yes:

your expectation still makes sense
and the mismatch itself is probably the main result
If you want, I can help turn that into a short “Findings” subsection text next.



Yes, that could make sense.

A plausible interpretation is:

News is more different from public
but that difference may be more domain-specific and more coherent in the directions that actually matter for News tasks
so even if the full weight update looks broad, only a small subset of directions may be functionally useful
then a low-rank adapter can capture enough of that useful subspace to perform well
So “more different” does not necessarily mean:

it needs more parameters
It can also mean:

it has a clearer domain shift
and the benchmark may reward only a compact part of that shift
That would fit the tension you observed:

structurally in weight space, News looks farther from public
functionally in eval, it still does well at low rank
A careful way to say it would be:

The News expert may be more strongly separated from the public model in weight space, yet still admit a low-rank approximation that preserves the domain-relevant directions needed for downstream performance. In that case, being “more different” from the public model would not imply needing more parameters, only that the useful adaptation may be concentrated in a smaller task-relevant subspace.

So yes, that hypothesis is reasonable. What you cannot conclude yet is that this is definitely the reason, only that it is consistent with the pattern you’re seeing.

Partly, yes.

What we do know about the News singular-value patterns from the saved analyses is:

the spectrum decays slowly

thresholding on singular values removed almost nothing
for at least the inspected real News layer, threshold retention was effectively 4096 / 4096
cumulative energy rises slowly

so energy_rank stays large unless tau is made quite low
compared with Math and Academic, News looks more spectrally diffuse

lower top50_energy_share
higher energy_rank_0.95
broader effective use of directions
in the probability view, News looked taller and narrower

so its singular-value probability distribution seems more concentrated in range
but not in a way that obviously explains the low eval rank by itself
So the broad spectral picture is:

News does not look sharply low-rank
it looks broad, slow-decaying, and hard to compress by simple tail-cutting rules
What we do not know yet is the more important part:

which of those singular directions are actually useful for the News eval tasks

What we know so far is:

pure spectrum-based rules do not match the eval story well
News looks farther from public in weight space
News also looks broader in SVD space:
higher energy ranks
lower top-k energy share
yet News still performed well at low LoRA rank in eval
So the main result is:

structural complexity in weights/SVD is not the same as task-useful complexity
The clearest patterns we have are:

News is more consistently separated from public
lower public_expert_cosine
higher relative_delta_norm
Math and Academic are more similar to each other
projection matters
gate_proj looks most distinctive
up_proj also seems informative
layer position matters
later layers often separate the experts more clearly
News is sometimes distinctive by being flatter, not just by having spikes
What that suggests for post hoc rank selection:

do not use one global expert-wide rank
move to at least per-projection, and ideally per-matrix, allocation
do not use SVD alone
combine:
similarity to public
relative delta magnitude
spectral spread / energy rank
A reasonable heuristic to try next is:

higher rank if:
low public_expert_cosine
high relative_delta_norm
high energy_rank_095
lower rank if:
high public_expert_cosine
low relative_delta_norm
concentrated spectrum
And weight that more strongly for:

gate_proj
up_proj
later layers
So the likely useful direction is a hybrid score, not a single threshold:

rank score = function of similarity + delta size + spectral spread
But the honest limitation is:

even that may still miss the News low-rank eval result
because the missing ingredient is probably task relevance of directions, not just their magnitude or count
So the best current conclusion is:

weights and SVDs can help identify where adaptation is larger and less compressible
they suggest projection- and layer-aware allocation
but by themselves they do not fully explain the best eval rank
especially for News
If you want, the next concrete step should be to build one first hybrid rank-allocation table with simple buckets, for example:

low
medium
high
per matrix, using public_expert_cosine, relative_delta_norm, and energy_rank_095.


In a post hoc LoRA setting, the most different expert need not require the highest rank. Structural distance from the public model may reflect broad weight-space change, while the task-relevant subspace may still be compact. Since training-time signals such as gradients or activation dynamics are unavailable, a more realistic strategy is to use matrix-aware post hoc heuristics based on weight similarity, delta magnitude, and spectral concentration, and then calibrate them against downstream evaluation.
