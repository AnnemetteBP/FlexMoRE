First-pass summary with candidate pool [8, 16, 32, 64, 128, 256, 512, 1024]:

Math: mean 614.9, median 512
News: mean 472.9, median 512
Academic: mean 575.6, median 512
So this first v04 guess is at least directionally closer to your expectation:

News comes out lower than Math
Academic sits between them, closer to Math
But it is still conservative overall, so this is probably a usable first draft rather than the final form. The next natural step would be tuning the bucket pool or score weights to push the allocation lower if we want News to separate more clearly.



First-pass summary with candidate pool [8, 16, 32, 64, 128, 256, 512, 1024]:

Math: mean 614.9, median 512
News: mean 472.9, median 512
Academic: mean 575.6, median 512
So this first v04 guess is at least directionally closer to your expectation:

News comes out lower than Math
Academic sits between them, closer to Math
But it is still conservative overall, so this is probably a usable first draft rather than the final form. The next natural step would be tuning the bucket pool or score weights to push the allocation lower if we want News to separate more clearly.


Math: max rank 28 -> 32
News: max rank 7 -> 8
Academic: max rank 18 -> 32

So News comes out lowest as expected. Likewise for Math and Academic which both come out higher.


I’d say it is better in the practical sense that it now matches your cross-expert expectation much more closely.

Meaning:

News gets the lowest rank
Math and Academic get higher ranks
and it gives one expert-wide rank, which is what the email wanted for practicality
So “better”:

yes, as a practical selection heuristic
not necessarily yes as a theoretically well-justified method
About the ranks:

The numbers we computed are actual LoRA ranks, not log2 values.

So:

8 means LoRA rank 8
32 means LoRA rank 32
They are chosen from the effective-rank outputs like this:

compute per-matrix effective_rank_v02
take the maximum across all expert matrices
round up to the next power of 2
So for example:

News: max effective rank 7 -> choose 8
Math: max effective rank 28 -> choose 32
Academic: max effective rank 18 -> choose 32
So this is not:

“log2 rank 1” directly
It is:

actual LoRA rank values on the usual scale:
1, 2, 4, 8, 16, 32, 64, ...
If you want the log2 form:

rank 8 = log2 rank 3
rank 32 = log2 rank 5
So the chosen ranks are:

News: 8
Math: 32
Academic: 32
That is low-to-higher rank allocation in the actual LoRA-rank space.


And the actual v06 expert-wide ranks are:

News -> 8
Math -> 32
Academic -> 16
So the answer to “what method was that improved analysis?” is now clean:

it is v06
and both the code and the results are named that way now.
